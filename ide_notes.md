# Работа с Git в VS Code vs Терминал

## Что удобно в VS Code (UI):
1. Наглядный просмотр изменений (diff) построчно с подсветкой добавленных и удалённых строк.
2. Удобный просмотр истории и графа коммитов через панель Source Control и GitLens.
3. Быстрое добавление файлов в stage и разрешение merge-конфликтов в визуальном редакторе.

## Что удобнее делать в терминале (CLI):
1. Сложные операции с историей и ветками (например, интерактивный `git rebase -i`, `cherry-pick`) и работа на удалённых серверах по SSH.# Работа с Git в VS Code vs Терминал

## Что удобно в VS Code (UI):
1. Наглядный просмотр изменений (diff) построчно с подсветкой добавленных и удалённых строк.
2. Удобный просмотр истории и графа коммитов через панель Source Control и GitLens.
3. Быстрое добавление файлов в stage и разрешение merge-конфликтов в визуальном редакторе.

## Что удобнее делать в терминале (CLI):
1. Сложные операции с историей и ветками (например, интерактивный `git rebase -i`, `cherry-pick`) и работа на удалённых серверах по SSH.